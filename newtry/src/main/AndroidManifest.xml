<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.newtry">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/duckicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/duckicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ReserveStatus"></activity>
        <activity android:name=".Reserve_NeworPre" />
        <activity android:name=".NewReserveActivity" />
        <activity android:name=".PreviousSettingsActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoadingActivity">
            <intent-filter>
                <action
                    android:name="android.intent.action.MAIN"
                    android:theme="@android:style/Theme.NoTitleBar.Fullscreen" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>